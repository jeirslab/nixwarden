# nixwarden (Python package)

The operator-side CLI of [jeirslab/nixwarden](https://github.com/jeirslab/nixwarden).
Build and run it through the flake; this directory is the source of the
`nixwarden` binary and its offline test suite. See the repository README.
